{
  "functions": {
    "api/books.js": { "memory": 512, "maxDuration": 10 }
  },
  "rewrites": [{ "source": "/api/(.*)", "destination": "/api/$1" }],
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist"
}
